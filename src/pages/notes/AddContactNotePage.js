import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddContactNote = () => {
  return (
    <View>
      <Text>AddContactNote</Text>
    </View>
  );
};

export default AddContactNote;

const styles = StyleSheet.create({});
