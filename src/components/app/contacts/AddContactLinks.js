import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddContactLinks = () => {
  return (
    <View>
      <Text>AddContactLinks</Text>
    </View>
  );
};

export default AddContactLinks;

const styles = StyleSheet.create({});
